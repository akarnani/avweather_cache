# Default values for avweather-cache

replicaCount: 1

image:
  repository: ghcr.io/akarnani/avweather-cache
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion
  tag: ""
  # Pinned at release time to sha256:... so the chart references an immutable image
  digest: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: false
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use
  name: ""

podAnnotations:
  prometheus.io/scrape: "true"
  prometheus.io/port: "9090"
  prometheus.io/path: "/metrics"

podSecurityContext:
  runAsNonRoot: true
  runAsUser: 65532
  fsGroup: 65532

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: true

service:
  type: LoadBalancer
  port: 8080
  # The metrics service is always type ClusterIP — never expose /metrics
  # externally on the LoadBalancer.
  metricsPort: 9090

config:
  server:
    port: 8080
  metrics:
    port: 9090
  cache:
    # Update interval for fetching new METAR data (Go duration format)
    updateInterval: "5m"
    # Source URL for METAR data
    sourceUrl: "https://aviationweather.gov/data/cache/metars.cache.xml.gz"

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi

livenessProbe:
  enabled: true
  httpGet:
    path: /metrics
    port: metrics
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  enabled: true
  httpGet:
    path: /metrics
    port: metrics
  initialDelaySeconds: 5
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 3

metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    interval: 30s
    scrapeTimeout: 10s
    # Additional labels for ServiceMonitor (used by Prometheus Operator for discovery)
    labels:
      prometheus: kube-prometheus

nodeSelector: {}

tolerations: []

affinity: {}
